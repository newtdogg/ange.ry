<div class="container-fluid">
  <div class="row">
    <div class="col-sm-2">
    </div>
    <div class="col-sm-8">
      <h1> <%= "user" %> </h1>
      <div class="row">
        <% "user".ratings.each |rating| %>
        <div class="col-sm-4">
          <p><%= Restaurant.find(rating.restaurant_id).name %></p>
          <ul>
            <li><p><%= rating.stars %></p></li>
            <li><p><%= restaurant.review %></p></li>
            <li><p><%= link_to 'Destroy', rating_path(id: rating.id),
                  method: :delete,
                  data:{ confirm: 'Are you sure?' } %>
          <p><%= link_to 'Edit', edit_review_path(id: rating.id) %></p>

            </p></li>
          </ul>
        </div>
        <% end %>
      </div>
    </div>
    <div class="col-sm-2">
    </div>
  </div>
</div>
